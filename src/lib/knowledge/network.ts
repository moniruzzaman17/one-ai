import "server-only";import { lookup } from "node:dns/promises";import { isIP } from "node:net";
function privateIp(address:string){if(address==="::1"||address.startsWith("fe80:")||address.startsWith("fc")||address.startsWith("fd"))return true;if(isIP(address)===4){const p=address.split(".").map(Number);return p[0]===10||p[0]===127||p[0]===0||(p[0]===169&&p[1]===254)||(p[0]===172&&p[1]>=16&&p[1]<=31)||(p[0]===192&&p[1]===168)||(p[0]===100&&p[1]>=64&&p[1]<=127)}return false}
export async function assertPublicUrl(value:string){const url=new URL(value);if(!["http:","https:"].includes(url.protocol)||url.username||url.password)throw new Error("Only public HTTP/HTTPS URLs are allowed");const records=await lookup(url.hostname,{all:true,verbatim:true});if(!records.length||records.some(r=>privateIp(r.address)))throw new Error("Private or local network addresses are not allowed");return url}
export async function safeFetch(input:URL,init:RequestInit={}){let url=await assertPublicUrl(input.toString());for(let i=0;i<5;i++){const response=await fetch(url,{...init,redirect:"manual",signal:AbortSignal.timeout(12_000),headers:{"User-Agent":"OneAI-KnowledgeBot/0.1 (+https://one.greenminds.info)",Accept:"text/html,text/plain,application/json,application/javascript,text/javascript,application/pdf;q=.8",...init.headers}});if(response.status>=300&&response.status<400){const next=response.headers.get("location");if(!next)throw new Error("Redirect without location");url=await assertPublicUrl(new URL(next,url).toString());continue}return response}throw new Error("Too many redirects")}
